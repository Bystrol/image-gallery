name: Deploy backend to Dev

on:
  push:
    branches: [ dev ]
    paths: [ 'backend/**' ]  # Only trigger when backend code changes
  pull_request:
    branches: [ dev ]
    paths: [ 'backend/**' ]
    types: [ closed ]

jobs:
  deploy:
    name: Deploy Backend to Dev
    runs-on: ubuntu-latest
    
    # Only run on merged PRs or direct pushes to dev
    if: github.event_name == 'push' || (github.event.pull_request.merged == true)
    
    defaults:
      run:
        working-directory: ./backend
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-north-1  # Match your serverless.yml region
      
      - name: Deploy to AWS
        run: npm run deploy:dev
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      
      - name: Comment deployment status
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🚀 Backend successfully deployed to **dev** environment!'
            })